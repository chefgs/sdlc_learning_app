# Render Blueprint Spec: Socratic Tutor Backend Only
# Import this file in the Render Dashboard (Blueprints section) to deploy the FastAPI container.

services:
  # ---------------------------------------------------------------------------
  # Python FastAPI Backend (WebSocket Container)
  # ---------------------------------------------------------------------------
  - type: web
    name: sdlc-learning-app-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend # Set the context to backend directory where requirements.txt resides
    plan: free
    healthCheckPath: /
    envVars:
      - fromGroup: sdlc-learning-app-secrets

# -----------------------------------------------------------------------------
# Environment Group for Shared Configurations & Secrets
# -----------------------------------------------------------------------------
envVarGroups:
  - name: sdlc-learning-app-secrets
